admin: chrismaltez
email: cflux.maltez@live.com
password: LiquidUserTest101$



for git:

git status
git add .
# ^ . will add all, otherwise specify file
#git add will put it "on deck"

git commit -m "blah blah"
# ^ -m flag will allow for message to be added.
# commit puts it in the "chamber"

git push -u origin master
# pushes the whole thing up.


git pull
# pulls it down from the repository